
一 什么是线性表

	线性表就是关系中的一对一的关系，如果是一对多就用树来表示，如果多对多就用网状来表示。

	线性表具有以下四个特征：

	1 有且只有一个“首”元素 
	2 有且只有一个“尾”元素 
	3 除“首”元素外，其余元素都有唯一的前驱元素。 
	4 除“尾”元素外，其余元素都有唯一的后继元素。

二 线性表的两种存储结构

	1 顺序表，即线性表用顺序存储结构保存数据，数据是连续的。 
	2 链表，即线性表用链式存储结构保存数据，数据不连续。



